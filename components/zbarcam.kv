#:import XCamera components.xcamera.XCamera

<ZBarCam>:
    Widget:
        id: proxy
        XCamera:
            id: xcamera
            play: True
            resolution: root.resolution
            allow_stretch: True
            keep_ratio: True
            center: self.size and proxy.center
            size: (proxy.width, proxy.height)
